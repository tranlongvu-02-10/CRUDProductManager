@model List<Product>

<h2 class="text-center my-4">Danh sách sản phẩm</h2>

<div class="container">

    <!-- Form lọc sản phẩm -->
    <form asp-action="Filter" method="post" class="row g-3 mb-4">
        <div class="col-md-4">
            <input type="text" name="ten" class="form-control" placeholder="Tên sản phẩm" />
        </div>
        <div class="col-md-3">
            <input type="number" step="0.01" name="minPrice" class="form-control" placeholder="Đơn giá từ" />
        </div>
        <div class="col-md-3">
            <input type="number" step="0.01" name="maxPrice" class="form-control" placeholder="Đơn giá đến" />
        </div>
        <div class="col-md-2">
            <button type="submit" class="btn btn-primary w-100">Lọc</button>
        </div>
    </form>
        <!-- Các nút sắp xếp và lọc -->
    <div class="mb-3 d-flex flex-wrap gap-2">
        <a asp-action="Index" class="btn btn-outline-secondary">Tải tất cả</a>
        <a asp-action="SortByDonGiaAsc" class="btn btn-outline-primary">Sắp xếp đơn giá ↑</a>
        <a asp-action="SortByDonGiaDesc" class="btn btn-outline-primary">Sắp xếp đơn giá ↓</a>
        <a asp-action="SortDonGiaAscThenMaDesc" class="btn btn-outline-info">Sắp xếp đơn giá ↑, mã ↓</a>
    </div>

    <!-- Bảng danh sách sản phẩm -->
    <table class="table table-bordered table-striped table-hover">
        <thead class="table-dark">
            <tr>
                <th scope="col">Mã</th>
                <th scope="col">Tên</th>
                <th scope="col">Đơn giá</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>@item.Ma</td>
                    <td>@item.Ten</td>
                    <td>@item.DonGia.ToString("N0") ₫</td>
                </tr>
            }
        </tbody>
    </table>
</div>
